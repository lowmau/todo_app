<h1>todos#index</h1>


	<% @todo.each do |i| %>
		<div class="well">
		<%= i.description %>
		<%= i.completed %>
		<%= link_to 'Mostrar', i, method: :get %>
		<%= link_to 'Editar', edit_path(i) %>
		<%= link_to 'Completar', complete_path(i) %>
		<%= link_to 'Borrar', i, method: :delete %>
		</div>
	<% end %>

	<%= link_to 'Create', todos_new_path %>
